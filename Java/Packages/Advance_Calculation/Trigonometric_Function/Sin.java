package Java.Packages.Advance_Calculation.Trigonometric_Function;

public class Sin {
    // Calculates sine value. Math.sin() expects angle in radians.
    public double findSin(double angleInRadians) {
        return Math.sin(angleInRadians);
    }
}
